# =============================================================================
# Rewrite API — Environment Configuration
# Copy this to .env and fill in your values
# =============================================================================

# --- Server ---
SERVER_PORT=8080
API_KEY=your-geomi-api-key-here
# TESTING_MODE=true          # Set to true to disable auth (dev only)

# --- Aptos ---
APTOS_NODE_URL=https://api.testnet.aptoslabs.com/v1
APTOS_CHAIN_ID=2             # 2=testnet, 1=mainnet

# --- Circle ---
CIRCLE_API_KEY=your-circle-api-key
CIRCLE_ENTITY_SECRET=your-32-byte-hex-entity-secret
CIRCLE_WALLET_SET_ID=your-wallet-setid

# CIRCLE_WALLETS is a JSON array of wallet objects.
# Each wallet needs wallet_id + address. public_key is optional (auto-fetched if missing).
# Example with one wallet:
CIRCLE_WALLETS=[{"wallet_id":"your-wallet-uuid","address":"0xYOUR_APTOS_ADDRESS","public_key":"0xYOUR_PUBLIC_KEY"}]

# --- Webhook (optional) ---
# WEBHOOK_URL=https://your-server.com/webhook

# --- Transaction Settings ---
MAX_GAS_AMOUNT=2000000
TXN_EXPIRATION_SECONDS=60
POLL_INTERVAL_SECONDS=5
STORE_TTL_SECONDS=180

# --- Orderless Transactions ---
# When true, transactions use replay-protection nonces instead of ordered
# sequence numbers. This avoids sequence-number contention for concurrent
# submissions. Can be overridden per-request via the "orderless" field.
ORDERLESS_ENABLED=true
NONCE_TTL_SECONDS=120        # How long used nonces are tracked (>= chain window)

# --- Idempotency ---
# Cached responses for idempotent requests are kept for this duration.
IDEMPOTENCY_TTL_SECONDS=300
